export const DEFAULT_HANDOFF_DIR = 'docs/session_handoffs'; export const DEFAULT_CONTRACT_FILE = 'docs/CONTRACT.md'; export const DEFAULT_BUS_FILE = 'docs/SESSION_BUS.md'; export function extractSessionIdFromPath(sessionFilePath: string): string { const filename = sessionFilePath.split('/').filter(Boolean).pop() ?? ''; const base = filename.replace(/\.jsonl$/, '').replace(/[^a-zA-Z0-9]/g, '_').toLowerCase(); return base ? `session_${base}` : `session_${filename}`; } export function buildHandoffBusPaths(sessionId: string) { const safe = sessionId.replace(/[^a-zA-Z0-9_-]/g, '_'); return { sessionId: safe, busFile: `${DEFAULT_HANDOFF_DIR}/${safe}/bus.md`, outputFile: `${DEFAULT_HANDOFF_DIR}/${safe}/output.md`, statusFile: `${DEFAULT_HANDOFF_DIR}/${safe}/status.json`, }; }
