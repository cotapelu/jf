--- src/code-indexer.ts
+++ src/code-indexer.ts
@@ -51,6 +51,7 @@ function parseTypeScript(content: string): CodeSymbol[] {
 	const symbols: CodeSymbol[] = [];
 	const funcRegex = /(?:function\s+(\w+)\s*\(|([a-zA-Z_$][\w$]*)\s*[:=]\s*(?:function\s*\(|\([^)]*\)\s*=>))/g;
 	let match: RegExpExecArray | null;
+	// biome-ignore lint/suspicious/noAssignInExpressions: standard RegExp exec pattern
 	while ((match = funcRegex.exec(content)) !== null) {
 		const name = match[1] || match[2];
 		if (!name) continue;
@@ -76,6 +77,7 @@ function parseTypeScript(content: string): CodeSymbol[] {
 	const symbols: CodeSymbol[] = [];
 	const classRegex = /(?:class|interface|type|enum)\s+(\w+)(?:\s+extends\s+\w+)?/g;
 	let match: RegExpExecArray | null;
+	// biome-ignore lint/suspicious/noAssignInExpressions: standard RegExp exec pattern
 	while ((match = classRegex.exec(content)) !== null) {
 		const name = match[1];
 		if (!name) continue;
@@ -111,6 +113,7 @@ function parsePython(content: string): CodeSymbol[] {
 	const symbols: CodeSymbol[] = [];
 	const funcRegex = /^(\s*)(?:async\s+)?def\s+(\w+)\s*\(/gm;
 	let match: RegExpExecArray | null;
+	// biome-ignore lint/suspicious/noAssignInExpressions: standard RegExp exec pattern
 	while ((match = funcRegex.exec(content)) !== null) {
 		const name = match[2];
 		if (!name) continue;
@@ -136,6 +139,7 @@ function parsePython(content: string): CodeSymbol[] {
 	const symbols: CodeSymbol[] = [];
 	const classRegex = /^(\\s*)class\\s+(\\w+)(?:\\s*\\([^)]*\\))?\\s*:/gm;
 	let match: RegExpExecArray | null;
+	// biome-ignore lint/suspicious/noAssignInExpressions: standard RegExp exec pattern
 	while ((match = classRegex.exec(content)) !== null) {
 		const name = match[2];
 		if (!name) continue;
